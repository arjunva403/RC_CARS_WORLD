<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RC WORLD - Order Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Aldrich&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'rc-red': '#df2f2f',
                        'rc-cyan': '#00cbf8',
                        'rc-gray': '#8b8b8b',
                        'rc-light-gray': '#f1eded',
                        'rc-border-gray': '#d9d9d9',
                        'rc-yellow': '#fbd300',
                        'rc-success': '#28a745',
                        'rc-error': '#dc3545'
                    },
                    fontFamily: {
                        'aldrich': ['Aldrich', 'sans-serif']
                    }
                }
            }
        }
    </script>
    <style>
        .sidebar-item.active {
            background-color: rgb(223 47 47);
            color: white;
        }
        .sidebar-item.active::before {
            content: '';
            display: block;
            width: 4px;
            height: 100%;
            background-color: rgb(223 47 47);
            position: absolute;
            left: 0;
            top: 0;
        }
        .order-item {
            transition: all 0.3s ease;
        }
        .order-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }
        .progress-step {
            position: relative;
            z-index: 2;
        }
        .progress-line {
            position: absolute;
            top: 16px;
            left: 0;
            right: 0;
            height: 2px;
            background-color: #d1d5db;
            z-index: 1;
        }
        .progress-line.active {
            background-color: #10b981;
        }
        .modal {
            backdrop-filter: blur(4px);
            animation: fadeIn 0.3s ease;
        }
        .modal-content {
            animation: slideIn 0.3s ease;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        @keyframes slideIn {
            from { transform: translateY(-50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        .status-badge {
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
    </style>
</head>
<body class="bg-white font-aldrich text-black">
    <!-- Toast Container -->
    <div id="toastContainer" class="fixed top-5 right-5 z-[9999]"></div>

    <!-- Header -->
    <header class="bg-rc-red text-white relative z-50">
        <div class="container mx-auto px-4">
            <div class="flex items-center justify-between py-4">
                <div class="flex items-center">
                    <a href="/logout" class="ml-2 text-base font-aldrich flex items-center space-x-2">
                        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                        </svg>
                    
                        <span>LOGOUT</span>
                    </a>
                </div>
                <div class="text-center">
                    <h1 class="text-2xl font-bold font-aldrich">RC WORLD</h1>
                    <p class="text-sm">small wheels, big thrills</p>
                </div>
                <div class="flex items-center gap-2">
                    <a href="/cart" class="relative text-white hover:text-rc-light-gray transition-colors" aria-label="Cart">
                        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M7 18c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12L8.1 13h7.45c.75 0 1.41-.41 1.75-1.03L21.7 4H5.21l-.94-2H1zm16 16c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2z"/>
                        </svg>
                        <span class="absolute -top-2 -right-2 bg-white text-rc-red text-xs font-semibold w-5 h-5 rounded-full flex items-center justify-center cart-badge">
                          
                        </span>
                    </a>
                    <span class="text-sm">MY CART</span>
                </div>
            </div>
        </div>
    </header>

    <!-- Navigation -->
    <nav class="bg-black text-white relative z-50">
        <div class="container mx-auto px-4">
            <div class="flex items-center justify-center space-x-8 py-3">
                <a href="/" class="hover:text-rc-red font-aldrich text-base transition-colors">HOME</a>
                <a href="/product" class="hover:text-rc-red font-aldrich text-base transition-colors">PRODUCTS</a>
                <a href="/contact" class="hover:text-rc-red font-aldrich text-base transition-colors">CONTACT US</a>
                <a href="/about" class="hover:text-rc-red font-aldrich text-base transition-colors">ABOUT US</a>
            </div>
        </div>
    </nav>

    <!-- Breadcrumbs -->
    <nav class="bg-rc-light-gray py-3 px-4 md:px-8 text-sm text-rc-gray">
        <a href="/" class="hover:underline">Home</a> &gt; 
        <a href="/profile" class="hover:underline">Account</a> &gt; 
        <a href="/myorders" class="hover:underline">My Orders</a> &gt; 
        <span class="text-black">Order Details</span>
    </nav>

    <!-- Main Content Area -->
    <main class="container mx-auto p-4 md:p-8 flex flex-col lg:flex-row gap-6">
        <!-- Left Sidebar -->
        <aside class="w-full lg:w-1/4 bg-white shadow-md rounded-lg p-6">
            <div class="flex items-center p-4 mb-6 bg-rc-light-gray rounded-lg">
                <div class="w-16 h-16 bg-rc-red rounded-full flex items-center justify-center text-white text-xl font-bold mr-4">
                    A
                </div>
                <div>
                    <p class="text-rc-gray">Hello,</p>
                    <p class="font-semibold text-lg font-aldrich">Arjun</p>
                </div>
            </div>
            
            <nav>
                <ul class="space-y-2">
                    <li class="py-3 px-4 hover:bg-rc-light-gray rounded-md cursor-pointer transition-colors">
                        <a href="/profile" class="flex items-center space-x-3 text-base font-medium text-rc-gray hover:text-black">
                            <i class="fas fa-user w-5"></i>
                            <span>Account Overview</span>
                        </a>
                    </li>
                    <li class="sidebar-item active relative py-3 px-4 rounded-md cursor-pointer">
                        <a href="/myorders" class="flex items-center space-x-3 text-base font-medium">
                            <i class="fas fa-box w-5"></i>
                            <span>My Orders</span>
                        </a>
                    </li>
                    <li class="py-3 px-4 hover:bg-rc-light-gray rounded-md cursor-pointer transition-colors">
                        <a href="/manageadd" class="flex items-center space-x-3 text-base font-medium text-rc-gray hover:text-black">
                            <i class="fas fa-map-marker-alt w-5"></i>
                            <span>Manage Addresses</span>
                        </a>
                    </li>
                    <li class="py-3 px-4 hover:bg-rc-light-gray rounded-md cursor-pointer transition-colors">
                        <a href="/wish" class="flex items-center space-x-3 text-base font-medium text-rc-gray hover:text-black">
                            <i class="fas fa-heart w-5"></i>
                            <span>My Wishlist</span>
                        </a>
                    </li>
                    <li class="py-3 px-4 hover:bg-rc-light-gray rounded-md cursor-pointer transition-colors">
                        <a href="/wallet" class="flex items-center space-x-3 text-base font-medium text-rc-gray hover:text-black">
                            <i class="fas fa-wallet w-5"></i>
                            <span>Wallet</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>

        <!-- Right Content - Order Details Section -->
        <section class="flex-grow bg-white shadow-md rounded-lg p-6 md:p-8">
            <!-- Order Header -->
            <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8">
                <div>
                    <h2 class="text-2xl font-bold font-aldrich mb-2">Order Details</h2>
                    <p class="text-rc-gray">
                        <i class="fas fa-calendar mr-2"></i>
                        Placed on <%= new Date(order.invoiceDate).toLocaleDateString('en-IN', { 
                            day: 'numeric', 
                            month: 'long', 
                            year: 'numeric',
                            hour: '2-digit',
                            minute: '2-digit'
                        }) %>
                    </p>
                </div>
                <div class="mt-4 md:mt-0">
                    <span class="text-sm text-rc-gray">Order ID:</span>
                    <span class="font-mono font-semibold text-black">#<%= order._id.toString().slice(-8).toUpperCase() %></span>
                </div>
            </div>

            <!-- Order Status Summary Cards -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                <!-- Delivery Address -->
                <div class="bg-gradient-to-br from-blue-50 to-blue-100 border border-blue-200 rounded-lg p-6">
                    <h3 class="text-lg font-semibold mb-4 text-blue-800 flex items-center">
                        <i class="fas fa-shipping-fast mr-2"></i>
                        Delivery Address
                    </h3>
                    <div class="text-sm text-blue-700">
                        <p class="font-semibold"><%= order.shippingAddress.title %></p>
                        <p class="mt-2"><%= order.shippingAddress.address %></p>
                        <p><%= order.shippingAddress.city %>, <%= order.shippingAddress.state %> - <%= order.shippingAddress.pincode %></p>
                        <p class="mt-2">
                            <i class="fas fa-phone mr-1"></i>
                            <%= order.shippingAddress.phone %>
                        </p>
                    </div>
                </div>

                <!-- Payment Information -->
                <div class="bg-gradient-to-br from-green-50 to-green-100 border border-green-200 rounded-lg p-6">
                    <h3 class="text-lg font-semibold mb-4 text-green-800 flex items-center">
                        <i class="fas fa-credit-card mr-2"></i>
                        Payment Information
                    </h3>
                    <div class="text-sm text-green-700">
                        <p class="font-semibold"><%= order.paymentMethod %></p>
                        <div class="mt-3">
                            <span class="inline-block px-3 py-1 rounded-full text-xs font-bold <%= order.paymentStatus.toLowerCase() === 'paid' ? 'bg-green-200 text-green-800' : order.paymentStatus.toLowerCase() === 'pending' ? 'bg-yellow-200 text-yellow-800' : 'bg-red-200 text-red-800' %>">
                                <i class="fas fa-<%= order.paymentStatus.toLowerCase() === 'paid' ? 'check-circle' : order.paymentStatus.toLowerCase() === 'pending' ? 'clock' : 'times-circle' %> mr-1"></i>
                                <%= order.paymentStatus.toUpperCase() %>
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Order Summary -->
                <div class="bg-gradient-to-br from-purple-50 to-purple-100 border border-purple-200 rounded-lg p-6">
                    <h3 class="text-lg font-semibold mb-4 text-purple-800 flex items-center">
                        <i class="fas fa-receipt mr-2"></i>
                        Order Summary
                    </h3>
                    <div class="text-sm text-purple-700 space-y-1">
                        <div class="flex justify-between">
                            <span>Subtotal:</span>
                            <span>₹<%= order.totalPrice %></span>
                        </div>
                        <div class="flex justify-between">
                            <span>Shipping:</span>
                            <span class="text-green-600 font-semibold">FREE</span>
                        </div>
                        <% if (order.discount && order.discount > 0) { %>
                        <div class="flex justify-between">
                            <span>Discount:</span>
                            <span class="text-green-600">-₹<%= order.discount %></span>
                        </div>
                        <% } %>
                        <div class="border-t border-purple-300 pt-2 mt-2">
                            <div class="flex justify-between font-bold">
                                <span>Total:</span>
                                <span>₹<%= order.finalAmount %></span>
                            </div>
                        </div>
                        <div class="mt-4">
                            <span class="status-badge inline-block px-3 py-1 rounded-full text-xs font-bold <%= order.orderStatus.toLowerCase() === 'delivered' ? 'bg-green-200 text-green-800' : order.orderStatus.toLowerCase() === 'shipped' ? 'bg-blue-200 text-blue-800' : order.orderStatus.toLowerCase() === 'cancelled' ? 'bg-red-200 text-red-800' : 'bg-yellow-200 text-yellow-800' %>">
                                <%= order.orderStatus.toUpperCase() %>
                            </span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Order Items -->
            <div class="mb-8">
                <h3 class="text-lg font-semibold mb-6 flex items-center">
                    <i class="fas fa-box-open text-rc-red mr-2"></i>
                    Order Items (<%= order.orderitems.length %>)
                </h3>
                <div class="space-y-4">
                    <% order.orderitems.forEach(item => { %>
                        <div class="order-item bg-white border border-rc-border-gray rounded-lg p-6">
                            <div class="flex flex-col md:flex-row gap-4">
                                <!-- Product Image -->
                                <div class="flex-shrink-0">
                                    <img src="<%= item.productId?.images?.[0] || 'https://via.placeholder.com/120' %>" 
                                         alt="<%= item.productId?.productName || 'Product' %>" 
                                         class="w-24 h-24 md:w-32 md:h-32 object-cover rounded-lg border border-rc-border-gray">
                                </div>
                                
                                <!-- Product Details -->
                                <div class="flex-grow">
                                    <h4 class="text-lg font-semibold mb-2 text-black">
                                        <%= item.productId?.productName || 'Unknown Product' %>
                                    </h4>
                                    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
                                        <div>
                                            <span class="text-rc-gray">Quantity:</span>
                                            <span class="font-semibold ml-1"><%= item.quantity %></span>
                                        </div>
                                        <div>
                                            <span class="text-rc-gray">Unit Price:</span>
                                            <span class="font-semibold ml-1">₹<%= item.price %></span>
                                        </div>
                                        <div>
                                            <span class="text-rc-gray">Total:</span>
                                            <span class="font-bold ml-1 text-rc-red">₹<%= item.price * item.quantity %></span>
                                        </div>
                                        <div>
                                            <span class="text-rc-gray">Status:</span>
                                            <div class="mt-1">
                                                <span class="inline-block px-2 py-1 rounded-full text-xs font-bold <%= item.status.toLowerCase() === 'delivered' ? 'bg-green-100 text-green-700' : item.status.toLowerCase() === 'shipped' ? 'bg-blue-100 text-blue-700' : item.status.toLowerCase() === 'cancelled' ? 'bg-red-100 text-red-700' : 'bg-yellow-100 text-yellow-700' %>">
                                                    <%= item.status.toUpperCase() %>
                                                </span>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <!-- Cancellation/Return Messages -->
                                    <% if (item.cancellationReason && item.cancellationReason !== 'none') { %>
                                        <div class="mt-3 p-3 bg-red-50 border border-red-200 rounded-lg">
                                            <p class="text-red-700 text-sm">
                                                <i class="fas fa-times-circle mr-1"></i>
                                                <strong>Cancelled:</strong> <%= item.cancellationReason %>
                                            </p>
                                        </div>
                                    <% } %>
                                    <% if (item.returnReason && item.returnReason !== 'none') { %>
                                        <div class="mt-3 p-3 bg-blue-50 border border-blue-200 rounded-lg">
                                            <p class="text-blue-700 text-sm">
                                                <i class="fas fa-undo mr-1"></i>
                                                <strong>Return Requested:</strong> <%= item.returnReason %>
                                            </p>
                                        </div>
                                    <% } %>
                                </div>
                                
                                <!-- Action Buttons -->
                                <div class="flex-shrink-0 flex flex-col gap-2">
                                    <% if (item.status === 'Pending' || item.status === 'Ordered') { %>
                                        <button class="bg-red-600 text-white py-2 px-4 rounded-lg font-semibold hover:bg-red-700 transition-colors text-sm" 
                                                onclick="openCancelModal('<%= item._id %>')">
                                            <i class="fas fa-times mr-1"></i>
                                            Cancel Item
                                        </button>
                                    <% } %>
                                    <% if (item.status === 'Delivered') { %>
                                        <button class="bg-orange-600 text-white py-2 px-4 rounded-lg font-semibold hover:bg-orange-700 transition-colors text-sm" 
                                                onclick="openReturnModal('<%= item._id %>')">
                                            <i class="fas fa-undo mr-1"></i>
                                            Return Item
                                        </button>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>

            <!-- Delivery Status Tracker -->
            <div class="bg-rc-light-gray rounded-lg p-6 mb-8">
                <h3 class="text-lg font-semibold mb-6 flex items-center">
                    <i class="fas fa-truck text-rc-red mr-2"></i>
                    Delivery Status
                </h3>
                <div class="relative">
                    <div class="progress-line"></div>
                    <div class="flex justify-between relative">
                        <% 
                            const statusMap = { 'ordered': 0, 'processed': 1, 'shipped': 2, 'in transit': 3, 'delivered': 4, 'cancelled': -1 };
                            const currentStatus = (order.orderStatus || '').toLowerCase();
                            const currentStep = statusMap[currentStatus] ?? 0;
                            const steps = [
                                { name: 'Order Placed', icon: 'fas fa-shopping-cart', dateOffset: 0 },
                                { name: 'Processed', icon: 'fas fa-cog', dateOffset: 1 },
                                { name: 'Shipped', icon: 'fas fa-truck', dateOffset: 2 },
                                { name: 'In Transit', icon: 'fas fa-shipping-fast', dateOffset: 3 },
                                { name: 'Delivered', icon: 'fas fa-check-circle', dateOffset: 4 }
                            ];
                            const baseDate = new Date(order.invoiceDate || Date.now());
                        %>
                        
                        <% steps.forEach((step, index) => {
                            let stepClass = '';
                            if (currentStep === -1) {
                                stepClass = '';
                            } else if (index < currentStep) {
                                stepClass = 'completed';
                            } else if (index === currentStep) {
                                stepClass = 'active';
                            }
                            const stepDate = new Date(baseDate);
                            stepDate.setDate(baseDate.getDate() + step.dateOffset);
                        %>
                        <div class="progress-step flex flex-col items-center text-center <%= stepClass %>">
                            <div class="w-12 h-12 rounded-full flex items-center justify-center text-white mb-2 <%= stepClass === 'completed' ? 'bg-green-600' : stepClass === 'active' ? 'bg-rc-red' : 'bg-rc-gray' %>">
                                <i class="<%= step.icon %>"></i>
                            </div>
                            <div class="text-sm font-medium <%= stepClass ? 'text-black' : 'text-rc-gray' %>">
                                <%= step.name %>
                            </div>
                            <div class="text-xs text-rc-gray mt-1">
                                <%= stepDate.toLocaleDateString('en-IN', { month: 'short', day: 'numeric' }) %>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                </div>
                
                <% if (currentStep === -1) { %>
                    <div class="mt-6 p-4 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-red-700 font-semibold">
                            <i class="fas fa-exclamation-triangle mr-2"></i>
                            This order has been cancelled.
                        </p>
                    </div>
                <% } else { %>
                    <div class="mt-6 p-4 bg-blue-50 border border-blue-200 rounded-lg">
                        <p class="text-blue-700 text-sm">
                            <i class="fas fa-info-circle mr-2"></i>
                            <strong>Expected Delivery:</strong> 
                            <%= new Date(new Date(order.invoiceDate).getTime() + 5 * 24 * 60 * 60 * 1000).toLocaleDateString('en-IN', { 
                                weekday: 'long',
                                day: 'numeric', 
                                month: 'long', 
                                year: 'numeric'
                            }) %>
                        </p>
                    </div>
                <% } %>
            </div>

            <!-- Action Buttons -->
            <div class="flex flex-col sm:flex-row gap-4">
                <a href="/product" class="flex-1">
                    <button class="w-full bg-rc-red text-white py-3 px-6 rounded-lg font-semibold hover:bg-rc-red/90 transition-colors">
                        <i class="fas fa-shopping-cart mr-2"></i>
                        Shop Again
                    </button>
                </a>
                <button class="flex-1 bg-white text-rc-gray border border-rc-border-gray py-3 px-6 rounded-lg font-semibold hover:bg-rc-light-gray transition-colors" 
                        onclick="window.print()">
                    <i class="fas fa-print mr-2"></i>
                    Print Invoice
                </button>
                <button class="flex-1 bg-rc-cyan text-white py-3 px-6 rounded-lg font-semibold hover:bg-rc-cyan/90 transition-colors"
                        onclick="downloadInvoice()">
                    <i class="fas fa-download mr-2"></i>
                    Download Invoice
                </button>
            </div>
        </section>
    </main>

    <!-- Return Modal -->
    <div id="returnModal" class="modal fixed inset-0 bg-black/50 z-[9999] hidden">
        <div class="modal-content bg-white mx-auto mt-20 p-6 rounded-xl max-w-md w-full mx-4 shadow-2xl">
            <div class="flex justify-between items-center border-b border-rc-border-gray pb-4 mb-6">
                <h3 class="text-xl font-semibold text-black">
                    <i class="fas fa-undo text-orange-600 mr-2"></i>
                    Request Return
                </h3>
                <button class="text-rc-gray hover:text-red-500 transition-colors text-xl p-1" onclick="closeModal('returnModal')">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <div class="mb-6">
                <label for="returnReason" class="block mb-3 font-semibold text-rc-gray">
                    <i class="fas fa-comment mr-2"></i>Reason for Return:
                </label>
                <textarea id="returnReason" 
                          placeholder="Please specify the reason for returning this item (e.g., damaged, wrong item, not as described)..." 
                          class="w-full p-3 border border-rc-border-gray rounded-lg focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-transparent min-h-[100px] resize-y"></textarea>
            </div>
            <div class="bg-orange-50 border border-orange-200 rounded-lg p-4 mb-6">
                <p class="text-orange-700 text-sm">
                    <i class="fas fa-info-circle mr-2"></i>
                    Return requests are processed within 24 hours. Items must be in original condition.
                </p>
            </div>
            <div class="flex gap-3">
                <button class="flex-1 bg-rc-light-gray text-rc-gray py-3 px-4 rounded-lg font-semibold hover:bg-rc-border-gray transition-colors" 
                        onclick="closeModal('returnModal')">
                    Cancel
                </button>
                <button class="flex-1 bg-orange-600 text-white py-3 px-4 rounded-lg font-semibold hover:bg-orange-700 transition-colors" 
                        onclick="submitReturn()">
                    <i class="fas fa-paper-plane mr-2"></i>
                    Submit Request
                </button>
            </div>
        </div>
    </div>

    <!-- Cancel Modal -->
    <div id="cancelModal" class="modal fixed inset-0 bg-black/50 z-[9999] hidden">
        <div class="modal-content bg-white mx-auto mt-20 p-6 rounded-xl max-w-md w-full mx-4 shadow-2xl">
            <div class="flex justify-between items-center border-b border-rc-border-gray pb-4 mb-6">
                <h3 class="text-xl font-semibold text-black">
                    <i class="fas fa-times-circle text-red-600 mr-2"></i>
                    Cancel Order Item
                </h3>
                <button class="text-rc-gray hover:text-red-500 transition-colors text-xl p-1" onclick="closeModal('cancelModal')">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <div class="mb-6">
                <label for="cancelReason" class="block mb-3 font-semibold text-rc-gray">
                    <i class="fas fa-comment mr-2"></i>Reason for Cancellation:
                </label>
                <textarea id="cancelReason" 
                          placeholder="Please specify the reason for cancelling this item (e.g., changed mind, found better price, no longer needed)..." 
                          class="w-full p-3 border border-rc-border-gray rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent min-h-[100px] resize-y"></textarea>
            </div>
            <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
                <p class="text-red-700 text-sm">
                    <i class="fas fa-exclamation-triangle mr-2"></i>
                    Once cancelled, this item cannot be restored to your order.
                </p>
            </div>
            <div class="flex gap-3">
                <button class="flex-1 bg-rc-light-gray text-rc-gray py-3 px-4 rounded-lg font-semibold hover:bg-rc-border-gray transition-colors" 
                        onclick="closeModal('cancelModal')">
                    Keep Item
                </button>
                <button class="flex-1 bg-red-600 text-white py-3 px-4 rounded-lg font-semibold hover:bg-red-700 transition-colors" 
                        onclick="submitCancel()">
                    <i class="fas fa-trash mr-2"></i>
                    Confirm Cancellation
                </button>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-rc-light-gray py-16">
        <div class="container mx-auto px-4">
            <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
                <div>
                    <h3 class="font-bold text-black mb-6 text-lg">CUSTOMER SERVICE</h3>
                    <ul class="space-y-3 text-rc-gray">
                        <li><a href="/contact" class="hover:text-black transition-colors">Contact us</a></li>
                        <li><a href="/return" class="hover:text-black transition-colors">Shipping & Returns</a></li>
                        <li><a href="/terms" class="hover:text-black transition-colors">Terms & Conditions</a></li>
                        <li><a href="/order" class="hover:text-black transition-colors">Delivery</a></li>
                    </ul>
                </div>
                <div>
                    <h3 class="font-bold text-black mb-6 text-lg">INFORMATION</h3>
                    <ul class="space-y-3 text-rc-gray">
                        <li><a href="/about" class="hover:text-black transition-colors">About</a></li>
                        <li><a href="/payment" class="hover:text-black transition-colors">Payments</a></li>
                        <li><a href="/size-guide" class="hover:text-black transition-colors">Size guide</a></li>
                        <li><a href="/admin" class="hover:text-black transition-colors">Administrator</a></li>
                    </ul>
                </div>
                <div>
                    <h3 class="font-bold text-black mb-6 text-lg">FOLLOW US</h3>
                    <div class="flex gap-4">
                        <a href="#" aria-label="Pinterest">
                            <svg class="w-8 h-8 text-rc-gray hover:text-black cursor-pointer transition-colors" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M12.017 0C5.396 0 .029 5.367.029 11.987c0 5.079 3.158 9.417 7.618 11.174-.105-.949-.199-2.403.041-3.439.219-.937 1.406-5.957 1.406-5.957s-.359-.72-.359-1.781c0-1.663.967-2.911 2.168-2.911 1.024 0 1.518.769 1.518 1.688 0 1.029-.653 2.567-.992 3.992-.285 1.193.6 2.165 1.775 2.165 2.128 0 3.768-2.245 3.768-5.487 0-2.861-2.063-4.869-5.008-4.869-3.41 0-5.409 2.562-5.409 5.199 0 1.033.394 2.143.889 2.741.097.118.112.221.085.345-.09.375-.293 1.199-.334 1.363-.053.225-.172.271-.402.165-1.495-.69-2.433-2.878-2.433-4.646 0-3.776 2.748-7.252 7.92-7.252 4.158 0 7.392 2.967 7.392 6.923 0 4.135-2.607 7.462-6.233 7.462-1.214 0-2.357-.629-2.75-1.378l-.748 2.853c-.271 1.043-1.002 2.35-1.492 3.146C9.57 23.812 10.763 24.009 12.017 24.009c6.624 0 11.99-5.367 11.99-11.988C24.007 5.367 18.641.001.012.001z"/>
                            </svg>
                        </a>
                        <a href="#" aria-label="Twitter">
                            <svg class="w-8 h-8 text-rc-gray hover:text-black cursor-pointer transition-colors" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M24 4.557c-.883.392-1.832.656-2.828.775 1.017-.609 1.798-1.574 2.165-2.724-.951.564-2.005.974-3.127 1.195-.897-.957-2.178-1.555-3.594-1.555-3.179 0-5.515 2.966-4.797 6.045-4.091-.205-7.719-2.165-10.148-5.144-1.29 2.213-.669 5.108 1.523 6.574-.806-.026-1.566-.247-2.229-.616-.054 2.281 1.581 4.415 3.949 4.89-.693.188-1.452.232-2.224.084.626 1.956 2.444 3.379 4.6 3.419-2.07 1.623-4.678 2.348-7.29 2.04 2.179 1.397 4.768 2.212 7.548 2.212 9.142 0 14.307-7.721 13.995-14.646.962-.695 1.797-1.562 2.457-2.549z"/>
                            </svg>
                        </a>
                        <a href="#" aria-label="LinkedIn">
                            <svg class="w-8 h-8 text-rc-gray hover:text-black cursor-pointer transition-colors" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
                            </svg>
                        </a>
                    </div>
                </div>
                <div>
                    <h3 class="font-bold text-black mb-6 text-lg">CONTACT US</h3>
                    <div class="space-y-3 text-rc-gray">
                        <div class="flex items-center gap-3">
                            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
                            </svg>
                            <span>(+91) 95551 76147</span>
                        </div>
                        <div class="flex items-center gap-3">
                            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/>
                            </svg>
                            <span>info@email.com</span>
                        </div>
                        <div class="flex items-center gap-3">
                            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zM12 20c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8z"/>
                                <path d="M12.5 7H11v6l5.25 3.15.75-1.23-4.5-2.67z"/>
                            </svg>
                            <span>8:00 - 21:00</span>
                        </div>
                    </div>
                </div>
            </div>
            <div class="flex justify-end mt-12 pt-8 border-t border-rc-border-gray">
                <div class="flex gap-6 items-center">
                    <div class="text-3xl font-bold text-rc-gray">VISA</div>
                    <div class="text-3xl font-bold text-rc-red">mastercard</div>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // Global variables
        let selectedItemId = null;

        // Toast Notification Function
        function showToast(message, type = 'success') {
            const toastContainer = document.getElementById('toastContainer');
            const toast = document.createElement('div');
            toast.className = `flex items-center gap-2 px-5 py-3 mb-3 rounded-lg text-white font-medium shadow-lg opacity-0 transform -translate-y-5 transition-all duration-300 ${type === 'success' ? 'bg-rc-success' : 'bg-rc-error'}`;
            toast.innerHTML = `
                <i class="fas fa-${type === 'success' ? 'check-circle' : 'exclamation-circle'}"></i>
                ${message}
                <button class="ml-auto text-white text-lg" onclick="this.parentElement.remove()">×</button>
            `;
            toastContainer.appendChild(toast);
            requestAnimationFrame(() => toast.classList.add('opacity-100', 'translate-y-0'));
            setTimeout(() => {
                toast.classList.remove('opacity-100', 'translate-y-0');
                setTimeout(() => toast.remove(), 300);
            }, 3000);
        }

        // Modal Functions
        function openReturnModal(itemId) {
            selectedItemId = itemId;
            const modal = document.getElementById('returnModal');
            modal.classList.remove('hidden');
            document.body.style.overflow = 'hidden';
            
            // Focus on textarea
            setTimeout(() => {
                document.getElementById('returnReason').focus();
            }, 300);
        }

        function openCancelModal(itemId) {
            selectedItemId = itemId;
            const modal = document.getElementById('cancelModal');
            modal.classList.remove('hidden');
            document.body.style.overflow = 'hidden';
            
            // Focus on textarea
            setTimeout(() => {
                document.getElementById('cancelReason').focus();
            }, 300);
        }

        function closeModal(modalId) {
            const modal = document.getElementById(modalId);
            modal.classList.add('hidden');
            document.body.style.overflow = 'auto';
            
            // Clear form data
            if (modalId === 'returnModal') {
                document.getElementById('returnReason').value = '';
            } else if (modalId === 'cancelModal') {
                document.getElementById('cancelReason').value = '';
            }
        }

        // Submit Return Request
        async function submitReturn() {
            const reason = document.getElementById('returnReason').value.trim();
            
            if (reason === '') {
                showToast('Please provide a reason for return', 'error');
                return;
            }

            if (reason.length < 10) {
                showToast('Please provide a more detailed reason (minimum 10 characters)', 'error');
                return;
            }

            try {
                const response = await fetch('/orderReturn', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ selectedItemId, reason })
                });

                const data = await response.json();

                if (data.success) {
                    showToast('Return request submitted successfully! We will process it within 24 hours.', 'success');
                    setTimeout(() => location.reload(), 2000);
                } else {
                    showToast(data.message || 'Something went wrong while submitting the return request', 'error');
                }
            } catch (error) {
                console.error('Error:', error);
                showToast('Network error. Please check your connection and try again.', 'error');
            }

            closeModal('returnModal');
        }

        // Submit Cancellation Request
        async function submitCancel() {
            const reason = document.getElementById('cancelReason').value.trim();
            
            if (reason === '') {
                showToast('Please provide a reason for cancellation', 'error');
                return;
            }

            if (reason.length < 10) {
                showToast('Please provide a more detailed reason (minimum 10 characters)', 'error');
                return;
            }

            try {
                const response = await fetch('/orderCancellation', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ selectedItemId, reason })
                });

                const data = await response.json();

                if (data.success) {
                    showToast('Cancellation request submitted successfully!', 'success');
                    setTimeout(() => location.reload(), 2000);
                } else {
                    showToast(data.message || 'Something went wrong while submitting the cancellation request', 'error');
                }
            } catch (error) {
                console.error('Error:', error);
                showToast('Network error. Please check your connection and try again.', 'error');
            }

            closeModal('cancelModal');
        }

        // Download Invoice Function
        function downloadInvoice() {
            showToast('Invoice download started!', 'success');
            // Here you would implement the actual download logic
            // For now, we'll just show a message
            setTimeout(() => {
                showToast('Invoice downloaded successfully!', 'success');
            }, 2000);
        }

        // Close modal when clicking outside
        document.addEventListener('click', function(event) {
            if (event.target.classList.contains('modal')) {
                if (event.target.id === 'returnModal') {
                    closeModal('returnModal');
                } else if (event.target.id === 'cancelModal') {
                    closeModal('cancelModal');
                }
            }
        });

        // Keyboard shortcuts
        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                if (!document.getElementById('returnModal').classList.contains('hidden')) {
                    closeModal('returnModal');
                } else if (!document.getElementById('cancelModal').classList.contains('hidden')) {
                    closeModal('cancelModal');
                }
            }
        });

        // Add smooth animations on page load
        document.addEventListener('DOMContentLoaded', function() {
            // Animate order items
            const orderItems = document.querySelectorAll('.order-item');
            orderItems.forEach((item, index) => {
                setTimeout(() => {
                    item.style.opacity = '0';
                    item.style.transform = 'translateY(20px)';
                    item.style.transition = 'all 0.5s ease';
                    
                    requestAnimationFrame(() => {
                        item.style.opacity = '1';
                        item.style.transform = 'translateY(0)';
                    });
                }, index * 100);
            });

            // Animate progress steps
            const progressSteps = document.querySelectorAll('.progress-step');
            progressSteps.forEach((step, index) => {
                setTimeout(() => {
                    step.style.opacity = '0';
                    step.style.transform = 'scale(0.8)';
                    step.style.transition = 'all 0.5s ease';
                    
                    requestAnimationFrame(() => {
                        step.style.opacity = '1';
                        step.style.transform = 'scale(1)';
                    });
                }, 500 + (index * 100));
            });
        });
    </script>
</body>
</html>
